# Canonical site URL (used for Open Graph and absolute receipt links)
NEXT_PUBLIC_SITE_URL=https://automind-ventures.vercel.app

# Business WhatsApp number (country code, no +)
NEXT_PUBLIC_WHATSAPP_NUMBER=2348055906616

# MongoDB connection string (Atlas recommended).
# Receipt images are stored in the user document when MongoDB is configured.
MONGODB_URI=
